[project]
name = "libtorrent-docs"
version = "0.1.0"
description = "Scraped mirror of libtorrent.org docs and blog"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4",
    "markdownify",
    "requests",
]

[tool.uv]
package = false
